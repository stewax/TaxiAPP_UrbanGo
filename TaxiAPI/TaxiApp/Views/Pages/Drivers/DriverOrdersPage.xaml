<UserControl x:Class="TaxiApp.Views.Pages.Drivers.DriverOrdersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaxiApp.Views.Pages.Drivers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#FFFFFF" CornerRadius="12" Padding="30" 
            BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="🚖 Доступные заказы" FontSize="24" FontWeight="Bold" 
                           Foreground="#212121"/>
                <TextBlock Text="Заказы, ожидающие водителя" FontSize="14" 
                           Foreground="#757575" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Кнопка обновления -->
            <Button Grid.Row="1" Content="🔄 Обновить список" Click="Refresh_Click"
                    Background="#FFC107" Foreground="#212121" 
                    FontSize="14" FontWeight="Bold" Padding="20,10" 
                    BorderThickness="0" Cursor="Hand" 
                    HorizontalAlignment="Left" Margin="0,0,0,15">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Список заказов -->
            <DataGrid Grid.Row="2" x:Name="OrdersGrid" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False" 
                      AlternatingRowBackground="#FAFAFA" 
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      BorderThickness="0" SelectionMode="Single">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FFF8E1"/>
                        <Setter Property="Foreground" Value="#212121"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,8"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="*"/>
                    <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="1.5*"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding ClientPhone}" Width="*"/>
                    <DataGridTextColumn Header="Откуда" Binding="{Binding PickupAddress}" Width="2*"/>
                    <DataGridTextColumn Header="Куда" Binding="{Binding DestinationAddress}" Width="2*"/>
                    <DataGridTextColumn Header="Стоимость" Binding="{Binding EstimatedCost, StringFormat={}{0} ₽}" Width="*"/>
                    <DataGridTextColumn Header="Время" Binding="{Binding CreatedAt, StringFormat=HH:mm}" Width="*"/>
                    <DataGridTemplateColumn Header="Действие" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Взять" Click="TakeOrder_Click"
                                        Background="#4CAF50" Foreground="White"
                                        Padding="10,5" BorderThickness="0" Cursor="Hand"
                                        Tag="{Binding Id}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
